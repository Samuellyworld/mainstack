// import global styles fro styled-components
import { createGlobalStyle } from "styled-components";

/// global styles
export const GlobalStyle = createGlobalStyle`
  *{ 
     padding: 0;
     margin: 0;
     box-sizing:border-box;   
     font-family: 'Poppins', sans-serif;
}
`;